<Page
    x:Class="SduPackage.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SduPackage"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource BackButtonBackgroundThemeBrush}" RequestedTheme="Light">

    <Grid>
        <FlipView Visibility="Collapsed">
            
        </FlipView>
        <Grid Background="{StaticResource BackButtonBackgroundThemeBrush}">
            <Grid.RowDefinitions>
                <RowDefinition Height="3*"/>
                <RowDefinition Height="7*"/>
            </Grid.RowDefinitions>
            <TextBlock Text="口袋山大" FontSize="50" Foreground="{ThemeResource TextBoxForegroundThemeBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <ProgressRing x:Name="isDowning" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" IsActive="True"/>
        </Grid>
    </Grid>
</Page>